# Loba Web

Frontend — submission form, dictionary, review queue.

## Phase 1
Vanilla HTML/CSS/JS — no build tools required.

## Phase 2
React (Vite) — migrated once component reuse justifies the tooling.

## Running locally
Open index.html in a browser. No server needed for Phase 1.
