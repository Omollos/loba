# Scripts

Utility scripts for data management.

| Script | Purpose |
|--------|---------|
| `export.go` | Generates nightly JSONL and CSV exports |
| `validate.go` | Checks entry quality before approval |
| `import.go` | Bulk import from CSV (for seeding) |

Scripts are written in Go and run independently of the main API.
